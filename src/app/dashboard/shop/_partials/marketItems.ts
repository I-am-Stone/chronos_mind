export interface MarketItem {
    id: string;
    name: string;
    price: number;
    image: string;
    quantity: number;
    category: string;
    description: string;
}
export const mockUserBalance = {
    gems: 20,
    gold: 22.49
};